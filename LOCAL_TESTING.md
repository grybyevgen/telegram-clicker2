# Локальное тестирование приложения

## Запуск через Live Server

Приложение можно тестировать локально в браузере через Live Server или любой другой локальный веб-сервер.

### Требования

1. **Firebase проект настроен** - убедитесь, что:
   - Firebase проект создан
   - Firestore база данных создана
   - Правила безопасности настроены (см. `FIRESTORE_RULES.md`)
   - Конфигурация Firebase в `app.js` правильная

2. **Локальный веб-сервер** - используйте один из вариантов:
   - **VS Code Live Server** (рекомендуется)
   - **Python**: `python -m http.server 8000`
   - **Node.js**: `npx http-server`
   - Любой другой локальный сервер

### Запуск через VS Code Live Server

1. Установите расширение "Live Server" в VS Code
2. Откройте папку проекта в VS Code
3. Правой кнопкой на `index.html` → "Open with Live Server"
4. Приложение откроется в браузере

### Режим разработки

Когда приложение запускается не в Telegram Web App, оно автоматически переходит в **режим разработки**:

- ✅ Показывается индикатор "🧪 Режим разработки"
- ✅ Используются тестовые данные пользователя
- ✅ Все функции работают как в Telegram
- ✅ Firebase подключение работает нормально

### Тестовые пользователи

Вы можете переключаться между разными тестовыми пользователями через URL параметр:

- `http://localhost:5500/index.html?testUser=1` - TestUser (ID: 123456789)
- `http://localhost:5500/index.html?testUser=2` - TestUser2 (ID: 987654321)
- `http://localhost:5500/index.html?testUser=3` - TestUser3 (ID: 555555555)

### Что работает в режиме разработки

✅ Все функции кликера  
✅ Магазин улучшений  
✅ Система энергии  
✅ Реферальная система  
✅ Лидерборд  
✅ Сохранение в Firebase  

### Что отличается от Telegram

⚠️ Вибрация и звуки отключены (только в Telegram)  
⚠️ Telegram Web App API недоступен  
⚠️ Используются тестовые данные пользователя  

### Отладка

В консоли браузера (F12) вы увидите:
- Логи инициализации Firebase
- Информацию о режиме разработки
- Все операции с Firestore
- Ошибки (если есть)

### Решение проблем

#### Ошибка подключения к Firebase

1. Проверьте конфигурацию Firebase в `app.js`
2. Убедитесь, что правила Firestore настроены (см. `FIRESTORE_RULES.md`)
3. Проверьте подключение к интернету
4. Откройте консоль браузера для деталей ошибки

#### CORS ошибки

Firebase модули загружаются через CDN. Если возникают CORS ошибки:
- Убедитесь, что используете локальный сервер (не открываете файл напрямую)
- Проверьте настройки браузера
- Попробуйте другой браузер

#### Firestore не инициализирован

1. Проверьте, что Firebase модули загрузились (см. консоль)
2. Убедитесь, что правила Firestore опубликованы
3. Проверьте конфигурацию Firebase

### Полезные команды в консоли

```javascript
// Проверить состояние приложения
window.debugUtils.getAppState()

// Проверить данные пользователя
window.userData

// Переинициализировать Firebase
await window.debugUtils.reinitFirebase()
```

### Переход в продакшн

Когда приложение запускается в Telegram Web App:
- Режим разработки автоматически отключается
- Используются реальные данные пользователя из Telegram
- Все функции работают как в продакшн
